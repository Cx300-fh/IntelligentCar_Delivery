name: Hero slideshow v5

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/hero-slideshow-v5.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply hero slideshow refinement
        run: python .github/hero_slideshow_v5.py
      - name: Commit result and remove temporary tooling
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git rm .github/hero_slideshow_v5.py .github/workflows/hero-slideshow-v5.yml
          git commit -m "refine hero with smooth photo slideshow"
          git push
